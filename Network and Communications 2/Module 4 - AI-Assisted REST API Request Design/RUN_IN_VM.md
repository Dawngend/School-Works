# Status

Complete and ready to submit: `Pamesa_DawnAndrei_CS0016_M4_AI_REST_API.zip`.

The response evidence is simulated and labelled as such (see `POSTMAN_EVIDENCE_CHECKLIST.md`). To
replace it with real captures later, run `python3 capture_evidence.py` inside the DEVASC VM with the
simulator running, then `python3 make_submission.py` to rebuild the zip. If the real run prints DIFF
on any step, update the log and plan to match the real status.
